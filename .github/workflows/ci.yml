name: CI

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "25"
          cache: "yarn"

      - name: Install dependencies
        run: yarn install

      - name: Install dependencies API
        run: yarn install --prefix=apps/api

      - name: Build API
        run: yarn build:api

      - name: Test API
        run: yarn test

      - name: Install dependencies WEB
        run: yarn install --prefix=apps/web

      - name: Build WEB
        run: yarn build:web
