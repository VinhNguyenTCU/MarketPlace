{
  "name": "marketplace",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix=apps/web",
    "dev:api": "npm run dev --prefix=apps/api",
    "dev": "concurrently -k --names web,api -c auto \"npm --prefix apps/web run dev\" \"npm --prefix apps/api run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
